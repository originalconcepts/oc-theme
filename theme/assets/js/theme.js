/**
 * Theme front end.
 *
 * Vanilla. No jQuery, no Slick, no Swiper. Carousels use CSS scroll-snap;
 * block behaviour ships with each block via block.json viewScript.
 */
document.documentElement.classList.add( 'oc-ready' );
