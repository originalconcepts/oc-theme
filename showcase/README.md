# Showcase page

The sales page at https://theme.mywebsite.co.il (Proginter site 1586, static).
Never part of the release zips. Deploy: a one-shot helper on the site pulls every path in manifest.txt
from raw.githubusercontent.com at a commit SHA.
